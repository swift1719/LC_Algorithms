class Solution {
public:
    bool canJump(vector<int>& nums) {
    int n=nums.size();
    int pos=0;
    
    for(int i=0;i<n;i++)
    {
        if(pos<i)
            return false;
        pos=max(pos,i+nums[i]);
                     
    }
    return true;
        
    }
};
